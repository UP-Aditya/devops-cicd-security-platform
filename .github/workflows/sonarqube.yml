name: SonarQube Scan

on:
  push:
    branches:
      - development
      - staging
      - production

jobs:
  sonar-scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: SonarQube Scan
        run: echo "Running SonarQube Analysis"